
version: 2

models:
    - name: core_trip
      description: "Trip Data"
      columns:
          - name: trip_id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null

    - name: core_order
      description: "A starter dbt model"
      columns:
          - name: order_id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null

    - name: core_customer
      description: "A starter dbt model"
      columns:
          - name: customer_id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null

    - name: core_aeroplane_details
      description: "A starter dbt model"
      columns:
          - name: aeroplane_id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null
